project:
  name: videocam-ai
  status: active
  priority: medium
repo:
  github: git@github.com:MikhailVBelyaev/videocam-ai.git
path: ~/Projects/videocam-ai
current_goal: 'TASK-001 scope for "Fix remaining Telegram image backlog problem" complete; awaiting human review. Created docs/TELEGRAM_BACKLOG_SCOPE.md with minimum deliverable (startup state initialization to latest image without sending), measurable acceptance criteria, explicit exclusions, and risks. No source code changes. All 146 tests pass. py_compile clean.'
next_actions:
- "Review docs/TELEGRAM_BACKLOG_SCOPE.md for the Telegram image backlog fix."
- "  - Verify minimum deliverable covers startup state initialization when .last_sent_file is missing."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer, object detection, or container infrastructure."
- "  - Decide whether to accept, revise, or reject the scope."
- "  - If accepted, prepare a TASK-002 design job."
- "Review the TASK-005 documentation diff for docs/TG_BOT_RUNBOOK.md."
- "  - Verify runbook accurately describes concurrency guard, per-iteration send cap,"
- "    cooldown bypass, and /admin latest image file send behavior."
- "  - Verify environment variables table includes MAX_IMAGES_PER_ITERATION and SEND_COOLDOWN_SECONDS."
- "  - Verify troubleshooting table covers static-scene silence, overlapping sender warnings,"
- "    and missing /admin photo."
- "  - Verify validation counts and operating steps are accurate."
- "  - Decide whether to accept, revise, or reject the documentation."
- "Review the TASK-004 QA diff for tests/test_tg_bot.py."
- "  - Verify sender lock-free positive case, cooldown-blocking negative case, sub-cap positive case."
- "  - Verify send_photo timestamp update on success and no-update on failure."
- "  - Verify _get_latest_image_path edge cases (no folders, empty folder, mtime ordering,"
- "    non-image filtering, OSError)."
- "  - Verify _get_latest_run_date OSError and _summarize_live_output no-media/OSError."
- "  - Decide whether to accept, revise, or reject the QA tests."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py, and README.md."
- "  - Verify _SENDER_LOCK prevents overlapping image_sender_job executions."
- "  - Verify MAX_IMAGES_PER_ITERATION (default 5) caps sends per iteration."
- "  - Verify SEND_COOLDOWN_SECONDS (default 300) bypasses duplicate filter when expired."
- "  - Verify admin_command() sends text summary followed by latest image file."
- "  - Verify graceful fallback on missing image or send failure."
- "  - Verify non-admin chats are silently ignored and existing /state behavior is unchanged."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review docs/TELEGRAM_IMAGE_DELIVERY_DESIGN.md for the Telegram image delivery fix."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers concurrency guard, send cap, cooldown"
- "    bypass, and /admin latest image send."
- "  - Verify key tradeoffs and risks are documented with adequate mitigations."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer,"
- "    object detection, or container infrastructure."
- "  - Decide whether to accept, revise, or reject the design."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review docs/TELEGRAM_IMAGE_DELIVERY_SCOPE.md for the Telegram image delivery fix."
- "  - Verify minimum deliverable covers: concurrency guard for image_sender_job,"
- "    per-iteration send cap (default 5), time-based duplicate bypass cooldown"
- "    (default 300s), and /admin sending the latest image file."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer,"
- "    or object detection model changes."
- "  - Decide whether to accept, revise, or reject the scope."
- "  - If accepted, prepare a TASK-002 design job."
- Review the TASK-005 documentation diff for `docs/WEB_VIEWER_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
  - Verify runbook accurately describes `/admin` behavior, static file serving,
    configuration, local development steps, Docker Compose operating steps,
    validation commands, and troubleshooting.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 implementation diff for `web_viewer/app.py`, `web_viewer/Dockerfile`,
  `web_viewer/requirements.txt`, `docker-compose.yml`, `tests/test_web_viewer.py`, and `README.md`.
  - Verify `/admin` renders HTML with run date, freshness, total/kept counts, car/person counts,
    missing expected count, and image links.
  - Verify missing/malformed `triage_summary.json` renders error message without crashing.
  - Verify existing static-file URLs (`/YYYY-MM-DD/frame.jpg`) continue to work (no regression).
  - Verify `docker-compose.yml` build context, volume mount, port mapping, and healthcheck.
  - Verify `py_compile` and unit tests pass.
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-002 design diff for `docs/WEB_ADMIN_PAGE_DESIGN.md`.
  - Verify affected services, modules, data flows, and interfaces are accurate.
  - Verify implementation approach (Flask replacing nginx) and tradeoffs are acceptable.
  - Verify risks and mitigations are adequate.
  - Decide whether to accept, revise, or reject the design.
  - If accepted, prepare a TASK-003 implementation job.
- Review the TASK-001 scope diff for `docs/WEB_ADMIN_PAGE_SCOPE.md`.
  - Verify minimum deliverable covers static file serving, `/admin` HTML page with
    triage stats (total images, kept images, car/person counts, missing expected
    objects), latest run date, freshness indicator, and links to latest images.
  - Verify acceptance criteria are measurable and exclusions are explicit.
  - Decide whether to accept, revise, or reject the scope.
  - If accepted, prepare a TASK-002 design job.
- Review the TASK-003 implementation diff for `tg_bot/bot.py`, `tests/test_tg_bot.py`,
  `tg_bot/requirements.txt`, `docker-compose.yml`, and `README.md`.
  - Verify `/state` command returns formatted summary with container names, running/exited/not-found
    status, health status, and uptime for the four expected containers.
  - Verify missing Docker socket yields "Container runtime unavailable. Docker socket not mounted?"
  - Verify non-admin chats are silently ignored.
  - Verify existing `/admin` behavior and image-sending behavior are unchanged (no regression).
  - Verify `py_compile` and unit tests pass.
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-002 design doc `docs/STATE_COMMAND_DESIGN.md`.
  - Verify affected services, modules, data flows, and interfaces are accurate.
  - Verify implementation approach (Docker SDK + read-only socket mount) and tradeoffs are acceptable.
  - Verify security mitigation (read-only mount, no control commands, admin restriction) is adequate.
  - Decide whether to accept, revise, or reject the design.
- Review the TASK-001 scope diff for `docs/STATE_COMMAND_SCOPE.md`.
  - Verify `/state` minimum deliverable covers container status for cams_grabber, tg_bot, sys_monitor, web_viewer.
  - Verify acceptance criteria are measurable and exclusions are explicit.
  - Verify Docker socket/security risks are documented.
  - Decide whether to accept, revise, or reject the scope.
- Review the TASK-005 documentation diff for `docs/TG_BOT_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
  - Verify runbook accurately describes `/admin` and `/state` behavior, env vars,
    Docker socket setup, operating steps, validation commands, and troubleshooting.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 implementation diff for `tg_bot/bot.py`, `tests/test_tg_bot.py`,
  `tg_bot/__init__.py`, and `README.md`.
  - Verify `/admin` command returns formatted summary with total_images, kept_images,
    car_count, person_count from latest `triage_summary.json`.
  - Verify missing/malformed `triage_summary.json` yields "No triage data available."
  - Verify non-admin chats are silently ignored.
  - Verify existing image-sending behavior is unchanged (no regression).
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-005 documentation diff for `README.md`, `docs/PROJECT_STATUS_MEMORY.md`,
  `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`, and `docs/PROJECT_MANAGER.yaml`.
  - Verify README accurately describes env vars, `/admin` behavior, and operating steps.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 Phase A implementation diff for `cams_grabber/snapshot_triage.py`,
  `tests/test_snapshot_triage.py`, `README.md`, and `docs/SNAPSHOT_TRIAGE_RUNBOOK.md`.
- Review the TASK-002 design doc `docs/TASK002_IMPROVE_IMAGE_QUALITY_STATISTICS_DESIGN.md`.
- Continue reviewing TASK-004/005 increments as needed.
next_tasks:
- Review `docs/TELEGRAM_IMAGE_DELIVERY_DESIGN.md` for the Telegram image delivery fix.
- Review `docs/TELEGRAM_IMAGE_DELIVERY_SCOPE.md` for the Telegram image delivery fix.
- Review the TASK-005 documentation diff for `docs/WEB_VIEWER_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
- Review the TASK-005 documentation diff for `docs/TG_BOT_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
- Review the TASK-003 implementation diff for the Telegram `/state` command.
- Review the TASK-002 design document `docs/STATE_COMMAND_DESIGN.md`.
- Review the TASK-001 scope document `docs/STATE_COMMAND_SCOPE.md`.
- Review the TASK-003 implementation for the Telegram `/admin` command.
- Review the TASK-003 Phase A implementation for correctness and completeness.
- Review the TASK-002 design doc for Phase B (enhanced quality metrics).
- Review the TASK-005 documentation diff for README.md, docs/PROJECT_STATUS_MEMORY.md,
  docs/NEXT_ACTIONS.md, docs/DEVELOPMENT_LOG.md, and docs/PROJECT_MANAGER.yaml.
- Verify that README accurately describes all implemented CLI flags and outputs.
- Verify that project status docs reflect the current implementation state.
risks:
- medium
risk: medium
risk_level: medium
ideas_count: 2
last_updated: '2026-06-19'
