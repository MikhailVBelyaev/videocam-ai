project:
  name: videocam-ai
  status: active
  priority: medium
repo:
  github: git@github.com:MikhailVBelyaev/videocam-ai.git
path: ~/Projects/videocam-ai
current_goal: 'TASK-004 QA validation for "Fix tg_bot still stuck on old LAST_SENT_FOLDER after fresh-first" complete; in review_required. Added 7 focused QA tests (TgBotFolderAdvanceQATests) in tests/test_tg_bot.py. All 219 tests pass (139 tg_bot + 52 snapshot_triage + 28 web_viewer); py_compile clean.'
next_actions:
- "Review the TASK-004 QA diff for tests/test_tg_bot.py."
- "  Verify TgBotFolderAdvanceQATests (7 tests) covers: state file write failure"
- "    (OSError does not block in-memory advancement), no dated folders (no advancement),"
- "    send_photo failure triggers advancement, empty current folder triggers advancement,"
- "    non-date directories ignored, admin state file unreadable, deleted LAST_SENT_FOLDER"
- "    falls back to latest folder."
- "  Decide whether to accept, revise, or reject the QA tests."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py, README.md, and docs/TG_BOT_RUNBOOK.md for the folder advancement fix."
- "  - Verify folder advancement block runs after send loop and before cleanup_old_folders()."
- "  - Verify sent_count == 0 and current_folder != newest_folder trigger."
- "  - Verify latest-folder boundary guard (stay put when current == newest)."
- "  - Verify LAST_SENT_IMAGE is cleared to None on advancement."
- "  - Verify .last_sent_file is written as new_folder/\\n on advancement."
- "  - Verify _format_admin_message() includes Watched folder, Newest folder, State file, and Status fields."
- "  - Verify existing concurrency guard, send cap, cooldown bypass, triage-aware selection, startup initialization, max-age filter, newest-first ordering, /state, and image-sending behavior are unchanged (no regression)."
- "  - Verify 8 new tests cover stale advance, similar advance, fully-sent advance, latest-folder stay-put, normal-send no-advance, multi-folder rapid advancement, admin stuck-state fields, and state file persistence."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review `docs/TG_BOT_FOLDER_ADVANCE_DESIGN.md`."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers: folder advancement when sent_count == 0"
- "    and current folder is not latest, latest-folder boundary guard, /admin stuck-state"
- "    fields (watched folder, newest folder, state file content, status indicator),"
- "    and state file persistence on advancement."
- "  - Verify key tradeoffs are documented with rationale (sent_count == 0 trigger"
- "    vs. alternatives, next-folder vs. jump-to-newest step size, folder/\\n format"
- "    vs. new file contract, direct read vs. reuse for /admin state file display)."
- "  - Verify risks and mitigations are adequate (overlap with pending reviews,"
- "    advancing past similarity-skipped images, multi-folder rapid advancement,"
- "    state file format parsing, write races)."
- "  - Verify no scope expansion into triage pipeline, web viewer, camera capture,"
- "    max-age filter, similarity threshold, cooldown behavior, startup initialization,"
- "    or Docker infrastructure."
- "  - Decide whether to accept, revise, or reject the design."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review `docs/TG_BOT_FOLDER_ADVANCE_SCOPE.md`."
- "  - Verify minimum deliverable covers: folder advancement when sent_count == 0 and"
- "    current folder is not latest, latest-folder boundary guard, /admin stuck-state"
- "    fields (watched folder, newest folder, state file content, status indicator)."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into triage pipeline, web viewer, camera capture,"
- "    max-age filter, similarity threshold, cooldown behavior, or Docker infrastructure."
- "  - Decide whether to accept, revise, or reject the scope."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py,"
- "  docs/TG_BOT_RUNBOOK.md, and README.md for the Telegram freshest-first fix."
- "  - Verify MAX_IMAGE_AGE_SECONDS env var (default 3600) with graceful fallback."
- "  - Verify _send_new_images_iteration() sorts remaining by mtime descending after"
- "    computing stable start_index."
- "  - Verify max-age filter skips stale images, increments _SKIPPED_STALE_COUNT,"
- "    and sets _LAST_SKIP_REASON = 'stale'."
- "  - Verify _LAST_SKIP_REASON is set for 'similar' and 'non-kept' skip paths."
- "  - Verify _format_admin_message() includes Skipped (stale), Backlog size,"
- "    Latest capture, Latest sent, and Last skip reason."
- "  - Verify existing concurrency guard, send cap, cooldown bypass, triage-aware"
- "    selection, startup initialization, /state, and image-sending behavior are"
- "    unchanged (no regression)."
- "  - Verify 12 new tests cover newest-first ordering, max-age filter, env var"
- "    handling, /admin extended fields, and backward compatibility."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review docs/TELEGRAM_FRESH_FIRST_DESIGN.md for the fresh-first design."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers newest-first processing, max-age filter,"
- "    and extended /admin counters/timestamps."
- "  - Verify key tradeoffs are documented with rationale."
- "  - Verify risks and mitigations are adequate."
- "  - Verify no scope expansion into triage pipeline, web viewer, camera capture,"
- "    persistent statistics, or Docker infrastructure."
- "  - Decide whether to accept, revise, or reject the design."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review the TASK-001 scope diff for docs/TELEGRAM_FRESH_FIRST_SCOPE.md."
- "  - Verify minimum deliverable covers newest-first processing, max-age filter,"
- "    and extended /admin counters."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Decide whether to accept, revise, or reject the scope."
- "  - If accepted, prepare a TASK-002 design job."
- "Review the TASK-005 documentation diff for docs/TG_BOT_RUNBOOK.md, README.md,"
- "  docs/PROJECT_STATUS_MEMORY.md, docs/NEXT_ACTIONS.md, docs/DEVELOPMENT_LOG.md,"
- "  and docs/PROJECT_MANAGER.yaml for the repeated-static fix."
- "  - Verify validation counts are accurate (115 tg_bot tests, 195 total)."
- "  - Verify troubleshooting entries for IMAGE_SIMILARITY_THRESHOLD misconfiguration"
- "    and non-kept counter behavior are accurate."
- "  - Verify startup behavior section, troubleshooting table, and command descriptions"
- "    remain accurate against tg_bot/bot.py."
- "  - Verify README.md Telegram bot section is consistent with implementation."
- "  - Decide whether to accept, revise, or reject the documentation."
- "Review the TASK-004 QA diff for tests/test_tg_bot.py."
- "  Verify TgBotTriageAwareQATests (6 tests) covers: cooldown bypass in kept/ mode,"
- "    non-kept counter stays zero without kept/ folder, all kept images skipped as"
- "    similar when cooldown not expired, OSError during non-kept counting swallowed,"
- "    dotfiles excluded from root fallback, send_photo called with kept/ subfolder path."
- "  Decide whether to accept, revise, or reject the QA tests."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py,"
- "  docs/TG_BOT_RUNBOOK.md, and README.md for the Telegram repeated static"
- "  image delivery fix."
- "  - Verify IMAGE_SIMILARITY_THRESHOLD env var (default 10) replaces hardcoded threshold=5."
- "  - Verify _kept_images_exist() checks for kept/ subfolder presence."
- "  - Verify _get_image_list() prefers kept/ images and falls back to all images."
- "  - Verify triage-aware _send_new_images_iteration() prefers kept/ images and skips"
- "    similar/duplicate images using IMAGE_SIMILARITY_THRESHOLD."
- "  - Verify _SENT_COUNT, _SKIPPED_DUPLICATE_COUNT, _SKIPPED_NON_KEPT_COUNT counters"
- "    are incremented correctly and displayed in /admin."
- "  - Verify send_photo() increments _SENT_COUNT."
- "  - Verify existing concurrency guard, send cap, cooldown bypass, /admin, /state,"
- "    and startup behaviors are unchanged (no regression)."
- "  - Verify 17 new tests cover kept-image helpers, triage-aware sender, threshold env"
- "    var, and send statistics."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review docs/TELEGRAM_REPEATED_STATIC_DESIGN.md for the Telegram repeated static"
- "  image delivery fix."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers triage-aware image selection (kept/"
- "    subfolder), configurable IMAGE_SIMILARITY_THRESHOLD (default 10), send"
- "    statistics counters, and /admin statistics display."
- "  - Verify key tradeoffs (kept/ vs JSON, always-show stats, threshold 10 vs 5,"
- "    non-kept counting, counter increment location) are documented with rationale."
- "  - Verify risks and mitigations are adequate."
- "  - Verify no scope expansion into triage pipeline, web viewer, camera capture,"
- "    staleness detection, persistent statistics, or Docker infrastructure."
- "  - Decide whether to accept, revise, or reject the design."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review docs/TELEGRAM_REPEATED_STATIC_SCOPE.md for the Telegram repeated static"
- "  image delivery fix."
- "  - Verify minimum deliverable covers triage-aware image selection (prefer kept/),"
- "    perceptual-hash threshold increase (default 10, configurable), and send"
- "    statistics counters in /admin."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into triage pipeline, web viewer, camera capture,"
- "    staleness detection, persistent statistics, or Docker infrastructure."
- "  - Decide whether to accept, revise, or reject the scope."
- "Review the TASK-005 documentation diff for docs/TG_BOT_RUNBOOK.md, README.md,"
- "  docs/PROJECT_STATUS_MEMORY.md, docs/NEXT_ACTIONS.md, docs/DEVELOPMENT_LOG.md,"
- "  and docs/PROJECT_MANAGER.yaml."
- "  - Verify validation counts are accurate (76 tg_bot tests, 156 total)."
- "  - Verify startup behavior section, troubleshooting table, and command descriptions"
- "    remain accurate against tg_bot/bot.py."
- "  - Verify README.md Telegram bot section is consistent with implementation."
- "  - Decide whether to accept, revise, or reject the documentation."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py, README.md, and docs/TG_BOT_RUNBOOK.md."
- "  - Verify _initialize_startup_state() scans the latest dated folder and sets"
- "    LAST_SENT_IMAGE/LAST_SENT_FOLDER to the most recently modified image."
- "  - Verify save_last_sent_file() is called during initialization to persist state."
- "  - Verify main() calls _initialize_startup_state() only when load_last_sent_file() returns (None, None)."
- "  - Verify existing behavior when .last_sent_file exists is unchanged (no regression)."
- "  - Verify existing concurrency guard, send cap, cooldown bypass, /admin, and /state behaviors are unchanged."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review docs/TELEGRAM_BACKLOG_SCOPE.md for the Telegram image backlog fix."
- "  - Verify minimum deliverable covers startup state initialization when .last_sent_file is missing."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer, object detection, or container infrastructure."
- "  - Decide whether to accept, revise, or reject the scope."
- "Review the TASK-005 documentation diff for docs/TG_BOT_RUNBOOK.md."
- "  - Verify runbook accurately describes concurrency guard, per-iteration send cap,"
- "    cooldown bypass, and /admin latest image file send behavior."
- "  - Verify environment variables table includes MAX_IMAGES_PER_ITERATION and SEND_COOLDOWN_SECONDS."
- "  - Verify troubleshooting table covers static-scene silence, overlapping sender warnings,"
- "    and missing /admin photo."
- "  - Verify validation counts and operating steps are accurate."
- "  - Decide whether to accept, revise, or reject the documentation."
- "Review the TASK-004 QA diff for tests/test_tg_bot.py."
- "  - Verify sender lock-free positive case, cooldown-blocking negative case, sub-cap positive case."
- "  - Verify send_photo timestamp update on success and no-update on failure."
- "  - Verify _get_latest_image_path edge cases (no folders, empty folder, mtime ordering,"
- "    non-image filtering, OSError)."
- "  - Verify _get_latest_run_date OSError and _summarize_live_output no-media/OSError."
- "  - Decide whether to accept, revise, or reject the QA tests."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py, and README.md."
- "  - Verify _SENDER_LOCK prevents overlapping image_sender_job executions."
- "  - Verify MAX_IMAGES_PER_ITERATION (default 5) caps sends per iteration."
- "  - Verify SEND_COOLDOWN_SECONDS (default 300) bypasses duplicate filter when expired."
- "  - Verify admin_command() sends text summary followed by latest image file."
- "  - Verify graceful fallback on missing image or send failure."
- "  - Verify non-admin chats are silently ignored and existing /state behavior is unchanged."
- "  - Decide whether to accept, revise, or reject the implementation."
- "Review docs/TELEGRAM_IMAGE_DELIVERY_DESIGN.md for the Telegram image delivery fix."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers concurrency guard, send cap, cooldown"
- "    bypass, and /admin latest image send."
- "  - Verify key tradeoffs and risks are documented with adequate mitigations."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer,"
- "    object detection, or container infrastructure."
- "  - Decide whether to accept, revise, or reject the design."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review docs/TELEGRAM_IMAGE_DELIVERY_SCOPE.md for the Telegram image delivery fix."
- "  - Verify minimum deliverable covers: concurrency guard for image_sender_job,"
- "    per-iteration send cap (default 5), time-based duplicate bypass cooldown"
- "    (default 300s), and /admin sending the latest image file."
- "  - Verify acceptance criteria are measurable and exclusions are explicit."
- "  - Verify no scope expansion into camera capture, triage pipeline, web viewer,"
- "    or object detection model changes."
- "  - Decide whether to accept, revise, or reject the scope."
- "  - If accepted, prepare a TASK-002 design job."
- Review the TASK-005 documentation diff for `docs/WEB_VIEWER_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
  - Verify runbook accurately describes `/admin` behavior, static file serving,
    configuration, local development steps, Docker Compose operating steps,
    validation commands, and troubleshooting.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 implementation diff for `web_viewer/app.py`, `web_viewer/Dockerfile`,
  `web_viewer/requirements.txt`, `docker-compose.yml`, `tests/test_web_viewer.py`, and `README.md`.
  - Verify `/admin` renders HTML with run date, freshness, total/kept counts, car/person counts,
    missing expected count, and image links.
  - Verify missing/malformed `triage_summary.json` renders error message without crashing.
  - Verify existing static-file URLs (`/YYYY-MM-DD/frame.jpg`) continue to work (no regression).
  - Verify `docker-compose.yml` build context, volume mount, port mapping, and healthcheck.
  - Verify `py_compile` and unit tests pass.
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-002 design diff for `docs/WEB_ADMIN_PAGE_DESIGN.md`.
  - Verify affected services, modules, data flows, and interfaces are accurate.
  - Verify implementation approach (Flask replacing nginx) and tradeoffs are acceptable.
  - Verify risks and mitigations are adequate.
  - Decide whether to accept, revise, or reject the design.
  - If accepted, prepare a TASK-003 implementation job.
- Review the TASK-001 scope diff for `docs/WEB_ADMIN_PAGE_SCOPE.md`.
  - Verify minimum deliverable covers static file serving, `/admin` HTML page with
    triage stats (total images, kept images, car/person counts, missing expected
    objects), latest run date, freshness indicator, and links to latest images.
  - Verify acceptance criteria are measurable and exclusions are explicit.
  - Decide whether to accept, revise, or reject the scope.
  - If accepted, prepare a TASK-002 design job.
- Review the TASK-003 implementation diff for `tg_bot/bot.py`, `tests/test_tg_bot.py`,
  `tg_bot/requirements.txt`, `docker-compose.yml`, and `README.md`.
  - Verify `/state` command returns formatted summary with container names, running/exited/not-found
    status, health status, and uptime for the four expected containers.
  - Verify missing Docker socket yields "Container runtime unavailable. Docker socket not mounted?"
  - Verify non-admin chats are silently ignored.
  - Verify existing `/admin` behavior and image-sending behavior are unchanged (no regression).
  - Verify `py_compile` and unit tests pass.
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-002 design doc `docs/STATE_COMMAND_DESIGN.md`.
  - Verify affected services, modules, data flows, and interfaces are accurate.
  - Verify implementation approach (Docker SDK + read-only socket mount) and tradeoffs are acceptable.
  - Verify security mitigation (read-only mount, no control commands, admin restriction) is adequate.
  - Decide whether to accept, revise, or reject the design.
- Review the TASK-001 scope diff for `docs/STATE_COMMAND_SCOPE.md`.
  - Verify `/state` minimum deliverable covers container status for cams_grabber, tg_bot, sys_monitor, web_viewer.
  - Verify acceptance criteria are measurable and exclusions are explicit.
  - Verify Docker socket/security risks are documented.
  - Decide whether to accept, revise, or reject the scope.
- Review the TASK-005 documentation diff for `docs/TG_BOT_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
  - Verify runbook accurately describes `/admin` and `/state` behavior, env vars,
    Docker socket setup, operating steps, validation commands, and troubleshooting.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 implementation diff for `tg_bot/bot.py`, `tests/test_tg_bot.py`,
  `tg_bot/__init__.py`, and `README.md`.
  - Verify `/admin` command returns formatted summary with total_images, kept_images,
    car_count, person_count from latest `triage_summary.json`.
  - Verify missing/malformed `triage_summary.json` yields "No triage data available."
  - Verify non-admin chats are silently ignored.
  - Verify existing image-sending behavior is unchanged (no regression).
  - Decide whether to accept, revise, or reject the implementation.
- Review the TASK-005 documentation diff for `README.md`, `docs/PROJECT_STATUS_MEMORY.md`,
  `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`, and `docs/PROJECT_MANAGER.yaml`.
  - Verify README accurately describes env vars, `/admin` behavior, and operating steps.
  - Verify project status docs reflect the current implementation state.
  - Decide whether to accept, revise, or reject the documentation.
- Review the TASK-003 Phase A implementation diff for `cams_grabber/snapshot_triage.py`,
  `tests/test_snapshot_triage.py`, `README.md`, and `docs/SNAPSHOT_TRIAGE_RUNBOOK.md`.
- Review the TASK-002 design doc `docs/TASK002_IMPROVE_IMAGE_QUALITY_STATISTICS_DESIGN.md`.
- Continue reviewing TASK-004/005 increments as needed.
next_tasks:
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py, README.md, and docs/TG_BOT_RUNBOOK.md for the folder advancement fix."
- "Review `docs/TG_BOT_FOLDER_ADVANCE_DESIGN.md` for the folder advancement fix."
- "  - Verify affected services, modules, data flows, and interfaces are accurate."
- "  - Verify implementation approach covers folder advancement, boundary guard,"
- "    /admin stuck-state fields, and state file persistence."
- "  - Verify key tradeoffs and risks are documented with adequate mitigations."
- "  - If accepted, prepare a TASK-003 implementation job."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py,"
- "  docs/TG_BOT_RUNBOOK.md, and README.md for the Telegram freshest-first fix."
- "Review the TASK-003 implementation diff for tg_bot/bot.py, tests/test_tg_bot.py,"
- "  docs/TG_BOT_RUNBOOK.md, and README.md for the Telegram repeated static image delivery fix."
- "Review the TASK-005 documentation diff for docs/TG_BOT_RUNBOOK.md, README.md,"
- "  docs/PROJECT_STATUS_MEMORY.md, docs/NEXT_ACTIONS.md, docs/DEVELOPMENT_LOG.md,"
- "  and docs/PROJECT_MANAGER.yaml for the Telegram backlog fix."
- "Review the TASK-003 implementation diff for `tg_bot/bot.py`, `tests/test_tg_bot.py`,"
- "  `README.md`, and `docs/TG_BOT_RUNBOOK.md` for the Telegram backlog fix."
- Review `docs/TELEGRAM_IMAGE_DELIVERY_DESIGN.md` for the Telegram image delivery fix.
- Review `docs/TELEGRAM_IMAGE_DELIVERY_SCOPE.md` for the Telegram image delivery fix.
- Review the TASK-005 documentation diff for `docs/WEB_VIEWER_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
- Review the TASK-005 documentation diff for `docs/TG_BOT_RUNBOOK.md`, `README.md`,
  `docs/PROJECT_STATUS_MEMORY.md`, `docs/NEXT_ACTIONS.md`, `docs/DEVELOPMENT_LOG.md`,
  and `docs/PROJECT_MANAGER.yaml`.
- Review the TASK-003 implementation diff for the Telegram `/state` command.
- Review the TASK-002 design document `docs/STATE_COMMAND_DESIGN.md`.
- Review the TASK-001 scope document `docs/STATE_COMMAND_SCOPE.md`.
- Review the TASK-003 implementation for the Telegram `/admin` command.
- Review the TASK-003 Phase A implementation for correctness and completeness.
- Review the TASK-002 design doc for Phase B (enhanced quality metrics).
- Review the TASK-005 documentation diff for README.md, docs/PROJECT_STATUS_MEMORY.md,
  docs/NEXT_ACTIONS.md, docs/DEVELOPMENT_LOG.md, and docs/PROJECT_MANAGER.yaml.
- Verify that README accurately describes all implemented CLI flags and outputs.
- Verify that project status docs reflect the current implementation state.
risks:
- medium
risk: medium
risk_level: medium
ideas_count: 2
last_updated: '2026-06-19'
